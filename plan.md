 I'm working on an iron beads template renderer the idea is to have a simple ASCII format that can be rendered into one or multiple components to put on the iron beads board. A simple template format is something that could be rendered by an LLM easily so it needs to be easily explained after the temple is rendered the user might want to save it so I guess whatever you have should be automatically saved in local storage and it also needs to be printable so we should use printing CSS and even if it's a black-and-white printer, I would still like to have something useful printed like showing a symbol or a letter for each bead and a legend that could work in black-and-white mode there's already a first first draft file, but I want to add typescript and vite and pnpm please set this all up